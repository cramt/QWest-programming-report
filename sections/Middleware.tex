% This section is about our clients and their features
% Dedicated client
%%% Possiblities - WPF(XAML), Winforms, etc.
%%% Reasoning over these

%%% Choice and reason for choice
%%% How did we use it? (implementation details)
% Web Client
%%% Possibilities - Webforms, MVC
%%%%% Different frameworks?
%%% Technologies in general we need to know: HTML, CSS, JS
%%% How did we use HTML, CSS, JS, JQuery, etc.
% Middleware
%%% What middleware are we using? What problems do we solve with the middleware
%%% (de)serializing JSON?
\chapter{Middelware}\label{ch:middleware}

QWest.Web bruger Owin \cite{Owin} til at leverer vores middleware. Det eneste stykke middleware skrevet manuelt er \texttt{QWest.Api.AuthenticationMiddleware} klassen, middleware'en checker om der er en authentikations token med i requestsen og hvis der er, hender den brugen som den token referer til og lægger den i OwinContext'en. 